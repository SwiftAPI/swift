imports:
  - { resource: src/swift/Application/services.yaml}

parameters:
# ...

services:
  _defaults:
    autowire: false
    autoconfigure: true
    public: true

  Swift\:
    resource: 'src/swift/*'
    exclude: [ 'Monolog/Logger' ]